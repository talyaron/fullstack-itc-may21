
// import { readAllUsers } from '../controllers/controllersUsers'

// export function isAdmin(req, res, next) {

//     const { email, password } = (req.body)

//     const allUsers:any = readAllUsers();

//     const isAdmin = allUsers.some(elem => (elem.email === email) && (elem.password === password))

//     if (!isAdmin) {
//         res.status(401).send({ error: 'No your not an admin' })
//     }

//     next()

// }


